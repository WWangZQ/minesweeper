# CNB 云原生配置 — minesweeper

# 云端开发环境：任意分支可在 CNB 网页打开在线 VSCode
# 用 node:20 作为环境镜像，这样云端终端自带 node / npm
$:
  vscode:
    - docker:
        image: node:20
      runner:
        cpus: 1
      services:
        - vscode
        - docker
      stages:
        # 进入环境时自动装好依赖，省得每次手动 npm install
        - name: 安装依赖
          script: npm install
        - name: 版本信息
          script: node -v && npm -v

# 构建流水线：push 到 main 时，用项目 Dockerfile 构建生产镜像并推送到 CNB Docker 制品库
main:
  push:
    - name: build-and-push
      runner:
        cpus: 1
      services:
        - docker
      stages:
        - name: 登录 CNB 制品库
          script: docker login "$CNB_DOCKER_REGISTRY" -u cnb -p "$CNB_TOKEN"
        - name: 构建镜像
          # Dockerfile 内部已执行 npm ci + npm run build（多阶段构建，产物镜像监听 3001）
          script: docker build -t "$CNB_DOCKER_REGISTRY/$CNB_REPO_SLUG_LOWERCASE:latest" .
        - name: 推送镜像
          script: docker push "$CNB_DOCKER_REGISTRY/$CNB_REPO_SLUG_LOWERCASE:latest"
